spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration

messaging:
  workload-queue: gym.workload.queue
  outbox:
    poll-delay: 5000
    batch-size: 100