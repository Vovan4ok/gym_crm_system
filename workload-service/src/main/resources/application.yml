server:
  port: 8081

jwt:
  secret: "some-random-jwt-secret-string-username-password"

spring:
  application:
    name: workload-service

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}